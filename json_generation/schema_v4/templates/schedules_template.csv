# schedules.csv -- the shift menu this roster may pick from.
# Lines starting with '#' and blank lines are ignored, so these notes can stay.
#
# A code stands for a grouping of hours and nothing else: this file IS the
# translation. The numbering is yours and means nothing on its own -- 9003 is
# 09:00-13:00 only because the row below says so. Need a grouping that is not here?
# Add a row with a new code.
#
# code                   what a result names in OutRosterTeamDays
# description            "HH:MM-HH:MM", or a label for a code with no window
# scheduleWeightMinutes  paid length in minutes. v4.0 has no break model, so this
#                        always equals endMin - startMin (see docs/FUTURE.md 2)
# startMin,endMin        the window in minutes from 00:00; endMin passes 1440 when
#                        the shift crosses midnight, so no reader has to infer it
#
# Every boundary must land on timeGrid.slotMinutes, or a solver on that grid cannot
# emit the code -- the validator warns about any that do not.
#
# A row with no window and zero weight is a REST SENTINEL, and that is how the
# validator recognises one: from the data, not a hardcoded list. Codes 1, 3 and 4
# keep the numbers WFM uses on import.
code,description,scheduleWeightMinutes,startMin,endMin
3,Day off,0,,
4,Vazio,0,,
9001,09:00-13:00,240,540,780
9002,13:00-17:00,240,780,1020
9003,09:00-17:00,480,540,1020
9004,22:00-06:00,480,1320,1800
